# 🔧 حل المشاكل الشائعة

## ❌ الأخطاء الشائعة وحلولها

### 1. `{"message":"Unauthorized"}`

**السبب:**
- لم ترسل Token في Header
- Token غير موجود أو منتهي الصلاحية
- JWT_SECRET غير موجود في `.env`

**الحل:**
```bash
# 1. تأكد من وجود JWT_SECRET في .env
JWT_SECRET=your-secret-key-here

# 2. سجل دخول للحصول على Token
POST /api/auth/login
{
  "username": "admin",
  "password": "admin123"
}

# 3. استخدم Token في Header
Authorization: Bearer <your-token>
```

---

### 2. `{"message":"User not found"}`

**السبب:**
- المستخدم غير موجود في قاعدة البيانات

**الحل:**
```bash
# أنشئ مستخدم جديد
npm run create-user admin admin123 SuperAdmin
```

---

### 3. `{"message":"Wrong password"}`

**السبب:**
- كلمة المرور خاطئة

**الحل:**
- تأكد من كلمة المرور الصحيحة
- أو أنشئ مستخدم جديد

---

### 4. `{"message":"Token expired"}`

**السبب:**
- Token منتهي الصلاحية (صالح لمدة 24 ساعة فقط)

**الحل:**
```bash
# سجل دخول مرة أخرى للحصول على token جديد
POST /api/auth/login
```

---

### 5. `{"message":"Forbidden"}`

**السبب:**
- ليس لديك الصلاحيات المطلوبة لهذا الـ endpoint

**الحل:**
- تأكد من أن دورك يسمح بهذا الإجراء
- SuperAdmin: جميع الصلاحيات
- Admin: صلاحيات محدودة
- Cashier: صلاحيات محدودة جداً

---

### 6. `{"message":"Server configuration error", "error":"JWT_SECRET is missing"}`

**السبب:**
- `JWT_SECRET` غير موجود في ملف `.env`

**الحل:**
```bash
# أضف JWT_SECRET في ملف .env
JWT_SECRET=your-very-secret-key-here-make-it-strong-and-random
```

---

### 7. `MongoDB connection error`

**السبب:**
- `MONGO_URI` غير صحيح
- قاعدة البيانات غير متاحة

**الحل:**
```bash
# تأكد من MONGO_URI في .env
MONGO_URI=mongodb://localhost:27017/vendly
# أو
MONGO_URI=mongodb+srv://username:password@cluster.mongodb.net/dbname
```

---

### 8. `Cannot find module '../models/...'`

**السبب:**
- مسار الملف خاطئ
- الملف غير موجود

**الحل:**
- تأكد من أن جميع الملفات موجودة
- أعد تشغيل السيرفر

---

### 9. `Port already in use`

**السبب:**
- المنفذ مستخدم بالفعل

**الحل:**
```bash
# غير PORT في .env
PORT=5001

# أو أوقف العملية التي تستخدم المنفذ
# Windows:
netstat -ano | findstr :5000
taskkill /PID <PID> /F

# Linux/Mac:
lsof -ti:5000 | xargs kill
```

---

### 10. `Validation error`

**السبب:**
- بيانات غير مكتملة في الطلب

**الحل:**
- تأكد من إرسال جميع الحقول المطلوبة
- راجع الـ API documentation

---

## 🔍 خطوات التشخيص

### 1. تحقق من السيرفر
```bash
# تأكد أن السيرفر يعمل
curl http://localhost:5000/
# يجب أن ترى: "Vendly Sales API Running"
```

### 2. تحقق من قاعدة البيانات
```bash
# تأكد من الاتصال بقاعدة البيانات
# يجب أن ترى في logs: "MongoDB Connected"
```

### 3. تحقق من المستخدمين
```bash
# أنشئ مستخدم إذا لم يكن موجود
npm run create-user admin admin123 SuperAdmin
```

### 4. تحقق من Token
```bash
# سجل دخول وتحقق من Token
curl -X POST http://localhost:5000/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{"username":"admin","password":"admin123"}'
```

### 5. تحقق من Headers
```bash
# تأكد من إرسال Authorization header بشكل صحيح
Authorization: Bearer <token>
# وليس
Authorization: <token>
```

---

## 📋 Checklist سريع

- [ ] السيرفر يعمل (`npm start`)
- [ ] MongoDB متصل
- [ ] ملف `.env` موجود ويحتوي على:
  - [ ] `MONGO_URI`
  - [ ] `JWT_SECRET`
  - [ ] `PORT`
- [ ] يوجد مستخدم في قاعدة البيانات
- [ ] Token صحيح وليس منتهي الصلاحية
- [ ] Authorization header موجود وصحيح
- [ ] الدور (role) لديه الصلاحيات المطلوبة

---

## 🆘 إذا استمرت المشكلة

1. **تحقق من Logs:**
   - افتح Terminal الذي يعمل فيه السيرفر
   - ابحث عن رسائل الخطأ

2. **تحقق من Network:**
   - تأكد من أن الطلب يصل للسيرفر
   - استخدم Postman أو curl للاختبار

3. **تحقق من الكود:**
   - تأكد من تحديث جميع الملفات
   - أعد تشغيل السيرفر

4. **افتح Issue:**
   - اذكر رسالة الخطأ بالضبط
   - اذكر الخطوات التي قمت بها
   - اذكر الـ request والـ response

---

## 💡 نصائح

1. **احفظ Token في متغير:**
   ```javascript
   let token = '';
   // بعد تسجيل الدخول
   token = response.data.token;
   ```

2. **استخدم Postman:**
   - أسهل للاختبار
   - يمكن حفظ Token تلقائياً

3. **تحقق من Token:**
   - Token صالح لمدة 24 ساعة
   - إذا انتهت صلاحيته، سجل دخول مرة أخرى

4. **استخدم Environment Variables:**
   - لا تضع secrets في الكود
   - استخدم `.env` دائماً

